const CreateEvent = () => (
  <div>
    <form action="">
      <label htmlFor="title">
        Title
        <input type="text" id="title" placeholder="Title" required />
      </label>
    </form>
  </div>
);

export default CreateEvent;
